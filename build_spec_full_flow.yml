version: 0.1
component: build
timeoutInSeconds: 6000
runAs: root
shell: bash
failImmediatelyOnError: true
steps:
  - type: Command
    name: "Test fail"
    command: |
      echo SHABBA $OCI_PRIMARY_SOURCE_URL
      exit 1
    onFailure:
      - type: Command
        command: |
          echo "RUNNING ON FAILURE"
